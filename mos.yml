author: Bradley Smith <bradley.1.smith@gmail.com>, pedalPusher68
type: lib
description: A Mongoose OS lib several formulas useful for developing sensor-based apps (temperature, pressure, etc.)
version: 1.0

libs_version: ${mos.version}
modules_version: ${mos.version}
mongoose_os_version: ${mos.version}

sources:
  - src

includes:
  - src

filesystem:
  - fs
config_schema:

libs:
#  - origin: https://github.com/mongoose-os-libs/i2c

tags:
  - c
  - js

manifest_version: 2017-09-29
